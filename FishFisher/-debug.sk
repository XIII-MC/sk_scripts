command /pl:
   trigger:
      stop

command /plugins:
   trigger:
      stop

command /bukkit:pl:
   trigger:
      stop

command /bukkit:plugins:
   trigger:
      stop

command /ver:
   trigger:
      stop

command /version:
   trigger:
      stop

command /about:
   trigger:
      stop

command /minecraft:me:
   trigger:
      stop